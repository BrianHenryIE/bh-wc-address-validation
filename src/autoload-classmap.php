<?php

return array(
	// Not entirely sure if this is necessary but shouldn't a problem (except if WooCommerce if installed to another directory).
	'WC_Email' => WP_PLUGIN_DIR . '/woocommerce/includes/emails/class-wc-email.php',
);
